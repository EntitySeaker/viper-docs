---
title: passwd
description: Viper command
sidebar:
    order: 32
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="note">Only the root user can change the password through passwd.</Aside>

### Purpose
Changes the password of a user.

```bash
passwd [USER] [PASSWORD]
```

### Arguments
- USER -- specify the user you want to change the password from
- PASSWORD -- specify the new password

### Examples
```bash
passwd volk new_password
```
Result:  
*Changed the password of "volk" to "new_password"*